# 2026-06-07 项目状态 & 任务清单

## 一、已完成工作

### 1. 缺失类补全（第一轮+第二轮）— 全部完成
- DBData.h/cpp, GameLogs.h/cpp, ItemEffect.h/cpp 扩展
- Curse.h/cpp, MutiConn.h 新增
- 所有反编译源中的类均已实现

### 2. 2019版本字段修正 — 全部完成
- ItemEffect 8个子类字段修正 + 新增 LingZhu, EquipJinHua

### 3. CfgData 重复代码清理
- 删除23个重复的 Init 函数定义（反编译伪代码 vs 2019干净版）
- 删除 2472 行冗余代码
- CfgData.cpp 从 25855 行降至 24685 行

### 4. CfgData Init 函数全部实现
- 42个之前缺失的 Init 函数全部实现为干净版
- 当前总计 247 个 Init 函数实现（含 int32_t 返回值的 InitXuWuTask/InitXinMoTask）
- cfgdata_new_decls.h 中所有声明均已实现
- 新增26个配置结构体到 CfgData.h
- 新增19个成员变量到 CfgData 类

### 5. 其他已完成
- CDBCFile.h/cpp 新增
- DataStructs.h 扩展（WorldBossInfo, KillerInfo, PlayerScoreRankGreater, MGLog）

### 6. DBService 2019多连接架构重构 — 已完成
- DBService 从继承 TcpService 改为管理多个 MutiConn<DBService> 连接
- MutiConn.h 重写为 TcpClient 子类，支持多连接转发
- DBService.h 重写：新增 ~30个函数声明，所有函数增加 int8_t connid 参数
- DBService.cpp 完全重写（~1700行）：onNetPacket 从 13 个 case 扩展到 28 个 case
- 注意：调用端（~20+文件）需逐步更新签名匹配

### 7. GameService 2019多连接架构重构 — 已完成
- GameService 从单连接改为管理多个 MutiConn<GameService>（m_mConn map）
- onNetPacket 签名改为 (GameConn* pConn, NetPacket* inPacket)，非virtual重载
- 新增 IndexMap, ChatValidate 结构体，MAX_GATE_CONNS=100
- 新增成员：m_id, m_userLock, m_ChatValidateMap, m_ChatValidateLock, m_dropControl, m_dropControlLock, m_serverBattle
- m_users 扩展为 [MAX_GATE_CONNS * MAX_CONNECTION] 支持多gate
- 新增函数（~40个）：
  - 网络：GetConn, GetAutoConnId, GetConnId, getUser, getUserPlayer, OnDBNetPacket
  - 发送：sendPacket(connid), sendPacketTo(connid), replySuccess, replyfailure
  - 广播：broadcastActivityState, broadcastToFamily, broadFamilyWarIcon, broadCastKiaFuHuoDongIcon, SendOpenBetaIcon, FamilyBroadcast
  - 业务：OnDaySwitch, onUpdatePay, RecalAllPlayerAttr, ResetFestivalData
  - Drop控制：InitDropTimes, GetDropTimes, AddDropTimes, UpdateDropTimes, ResetDropTimes, ClearDropTimes, OnDropTimesLoaded, CheckDropLimit, OnDropRecordLoaded, OnDropRecordSaved
  - Chat验证：SendChatValidate, GetChatValidate, CheckChatValidateChatSend, OnChatValidateResult
  - 城战：UpdateCityWarTitle, UpdateCityActState, SetFamilyMemberNeedSync, GetFamilyMemberInMap, broadcastCityWarWinnerLogin
  - 世界Boss/跨服：InitServerBattle, SetServerBattle, InitMoYuShiJieRecord, SendMoYuShiJieRecord, AddMoYuShiJieRecord
  - 禁言/踢人：OnSocialCityMasterBanChat, GetCityMasterBanChatTime, KickAll
  - 其他：onCheckTeShuTitle, UpdateTencentInfo, SetWorldLevel, onUpdateActivityState
- onNetPacket 消息处理扩展到 22 个 case（含 0x4E81-0x4E8E, 0x4EC1, 0x4EC4）
- 旧函数签名兼容：popNetpacket(connid), broadcast(IndexMap), onPlayerSaved(connid,...)

### 8. EquipManager 2019装备系统扩展 — 已完成
- 新增 ServerNewId 结构体（nNextId/nMaxId），ID缓存机制避免每次查DB
- 新增 m_mNewId map + m_IdLock 读写锁
- UpdateMemEquip/DeleteMemEquip 增加 connid + nReason 参数（保留旧签名兼容重载）
- SendPlayerEquipInfo 重构为3个重载：Player指针版 + 单装备版 + 向量版
- 旧 `const Player&` 版本保留为内联兼容包装
- 新增 BroadcastEquipInfo — 世界广播装备信息
- getEquipId 改为缓存机制 + getEquipIdFromDB 批量获取ID范围
- getEquipIdFromDB 区分跨服/非跨服模式（line==9时serverId=0）
- 所有发送函数使用 MemEquip::PackageClientData + 多连接感知发送（connid+gateIndex）

### 9. PetManager 2019宠物系统扩展 — 已完成
- 新增 PetNewId 结构体（同 EquipManager 的 ServerNewId），m_mNewId + m_IdLock 缓存宠物ID
- 新增 m_mPetRank（map<PetId_t, int16_t>）— 宠物排名缓存
- 新增 GetPetRankIndex(nPetId) — 查排名索引
- OnUpdatePetRank/OnInitPetRank 重写：简化为 PetId+RankIndex（不再读取10个属性排名）
- getPetId 改为缓存机制 + getPetIdFromDB 批量获取ID范围
- getPetIdFromDB 区分跨服/非跨服模式（line==9时serverId=0）
- DB_SERVICE.insertMemPet/updateMemPet/deleteMemPet 调用已适配 connid 参数

### 11. EquipBack 2019装备找回 — 已完成
- Init 从DB加载 mem_equip_back_record（最近50条）+ mem_equip_back_depot
- SendEquipBackInfo/OnRecord/CountChange/LimitChange 改为多连接感知（connid + gateIndex）
- OnGoldBack/OnBuyBack 改用 RemoveItem + popNetpacket(connid) + worldBroadcast
- KiaFuEquipBack 改用 TranseCurrencyItem + OnSendSysMail 7参数版
- 新增 IACR_EQUIP_BUY_BACK / IDCR_EQUIP_GOLD_BACK / IDCR_KAI_HUO_EQUIP_BACK 枚举
- Shared.h 新增 TranseCurrencyItem 函数（CURRENCY_TYPE → CURRENCY_ITEM_ID）
- PDUDefine.h 新增 SM_FAMILY_SOCIAL_FINISH_BOSS

### 12. FamilyManager 2019家族系统 — 已完成
- Init 增加 member_card/leader_cid/medl_level/territory_open 字段加载 + 查族长名字 + mem_family_dungeon
- 新增 OnInitFamilyInfo — 批量加载家族信息（使用 UnPackageData）
- OnUpdateFamilyInfo 改用 UnPackageData 替代手动读取
- 新增 GetTerritoryState / GetMedlLevel — 查领地状态和勋章等级
- 新增 StartfamilyDungeon — 家族副本启动（难度/次数/DB保存）
- 新增 SaveFamilyDungeonStuMap — 家族副本数据DB持久化
- 新增 DeleteFamilyDungeon — 删除副本 + 通知
- 新增 SetFamilyDungeonHard — 设置最大完成难度
- 新增 FamilyDungeonStu 结构体 + m_FamilyDungeonStuMap / m_FamilyDungeonMap
- SendSocialFinishBoss 改为多连接感知

### 13. OpenBeta 2019公测活动 — 已完成
- 从265行空壳扩展到1557行完整实现（OpenBeta.h 238行 + OpenBeta.cpp 1319行）
- Init: 完整读取 OpenBetaAct.cfg 配置（CONFIG/RECHARGE/CHOU_JIANG/COLLECT_DROP/BOSS/DAILY_LIMIT_SHOP/TITLE/SHI_ZHUANG/RED_PACK/XIAO_FEI_SUM_RANK/OBT_FLOP/OBT_LI_QUAN/OBT_QIAN_DAO/OBT_YAN_HUA/OBT_GUO_QING_TASK/OBT_GUO_QING_REWARD）
- ShopGoods 改为 nPrice + vItem（替代旧 nGold/nReGold/nCount/nLeftCount）
- FestivalRank 改为 nCharId/nScore/nTime/bChange（替代旧 nCid/nValue）
- 新增多连接感知：m_mXiaoFeiSumRank（map<int8_t, vector>）按 connid 分区 + m_lock 互斥锁
- 新增方法：GetQianDaoReward / broadcastBossStart / broadcastBossEnd / getIconState / getBossIconState
- 完整实现：BOSS刷新/击杀/广播、在线奖励、充值奖励、3种商店、翻牌、礼券、签到、烟花、消费排行
- DataStructs.h: +ICR_OPEN_BETA 枚举
- CfgData.h: +GetMapMonsterId() / +m_MapMonsterId
- ItemHelper.h: +parseItemDataString()

### 11. EquipBack 2019装备找回 — 已完成
- Init 从DB加载 mem_equip_back_record（最近50条）+ mem_equip_back_depot
- SendEquipBackInfo/OnRecord/CountChange/LimitChange 改为多连接感知（connid + gateIndex）
- OnGoldBack/OnBuyBack 改用 RemoveItem + popNetpacket(connid) + worldBroadcast
- KiaFuEquipBack 改用 TranseCurrencyItem + OnSendSysMail 7参数版
- 新增 IACR_EQUIP_BUY_BACK / IDCR_EQUIP_GOLD_BACK / IDCR_KAI_HUO_EQUIP_BACK 枚举
- Shared.h 新增 TranseCurrencyItem 函数（CURRENCY_TYPE → CURRENCY_ITEM_ID）
- PDUDefine.h 新增 SM_FAMILY_SOCIAL_FINISH_BOSS

### 12. FamilyManager 2019家族系统 — 已完成
- Init 增加 member_card/leader_cid/medl_level/territory_open 字段加载 + 查族长名字 + mem_family_dungeon
- 新增 OnInitFamilyInfo — 批量加载家族信息（使用 UnPackageData）
- OnUpdateFamilyInfo 改用 UnPackageData 替代手动读取
- 新增 GetTerritoryState / GetMedlLevel — 查领地状态和勋章等级
- 新增 StartfamilyDungeon — 家族副本启动（难度/次数/DB保存）
- 新增 SaveFamilyDungeonStuMap — 家族副本数据DB持久化
- 新增 DeleteFamilyDungeon — 删除副本 + 通知
- 新增 SetFamilyDungeonHard — 设置最大完成难度
- 新增 FamilyDungeonStu 结构体 + m_FamilyDungeonStuMap / m_FamilyDungeonMap
- SendSocialFinishBoss 改为多连接感知

### 10. WorldBoss 2019世界Boss逻辑重构 — 已完成
- 新增 InitDBInfo — 从 mem_world_boss_info 加载Boss等级/经验/击杀者到内存
- 新增 InitDropRecord — 从 mem_drop_record 加载掉落记录（special 5条 + 普通 30条）
- 新增 m_dropRecords / m_dropRecordsSpecial / m_RecordLock
- 新增 DropRecord 结构体（strName, nCharId, nMapId, nMid, nRecord, nTime, nSpecial）
- Init 增加 InitDBInfo/InitDropRecord 调用，使用 CfgData::GetMonsterReviveTime
- OnAddBoss — 创建Boss怪物（含等级属性调整 adjustBossAttr）
- OnBossSummon — Boss复活，签名增加 Map*，增加 saveBossInfo
- OnBossKilled — Boss被杀，签名增加 Map*，含等级提升、RuinsBoss通知、saveBossInfo
- adjustBossAttr — 根据等级调整Boss属性（映射vAttr到CfgMonster字段）
- saveBossInfo / UpdateWorldBossInfo — DB持久化
- AddDropRecord / SendDropRecord — 掉落记录添加和发送（多连接）
- SendRuinsBossInfo / UpdateRuinsBossInfo — 遗迹Boss信息（多连接）
- PacketBossInfo 移除 m_IsShow 检查
- SendWorldBossIcon 改用 Player::SendIconState
- CfgData.h: 新增 GetMonsterAdjust/GetMonsterReviveTime 声明，m_mMonsterAdjust key改为pair<int,int>
- CfgDropRecord 新增 nSpecial/nItemClass 字段
- Monster.h/cpp: 新增 OnLevelUp 方法
- PDUDefine.h: 新增 SM_SEND_DROP_RECORD/SM_SEND_RUINS_BOSS_INFO/SM_SEND_RUINS_BOSS_CHANGE

### 11. 项目清理 + MEMORY.md重写 — 已完成
- 删除 Windows 残留文件：targetver.h, stdafx.cpp, resource.h
- 删除备份文件：Activity.cpp.bak
- 删除临时文件：cfgdata_new_decls.txt
- 删除 16 个一次性 Python 脚本（/home/gameserver/*.py）
- MEMORY.md 重写为完整项目级文档（含待做清单）
- 推送到 game1 仓库

---

## 二、待完成任务

### 任务 A: 反编译风格函数重写（86个）— **进行中**
CfgData.cpp 机械清理+手动重写进度：
- **Batch 1完成**：IDA寄存器注释清除，24743→23514行
- **Batch 2完成**：
  - _M_node引用：38→0（全部清除）
  - _Rb_tree_iterator类型声明：41→0（全部清除）
  - _List_iterator/_List_const_iterator：全部清除
  - LODWORD/HIDWORD：33→0（全部清除）
- 完整重写的函数：InitGameTable, fetchItem, RandXiangYaoTaskId, GetSuperBuff, Get37wanSuperBuff
- 35+个getter函数已手动重写
- 当前行数：22406行（从24743减少2337行）
- fetchActivity反编译版删除（707行），干净版已存在于3017-3344行
- InitTouZiTable中最后1个LODWORD替换为stu.nNoticeId

### 任务 B: 已修改文件 2019 逻辑完善

| 文件 | 状态 | 说明 |
|------|------|------|
| DBService.cpp/h | **已完成** | 2019多连接架构重构完成 |
| GameService.cpp/h | **已完成** | 2019多连接架构重构完成：~40新函数，多gate支持 |
| EquipManager.cpp/h | **已完成** | 2019装备系统扩展：ID缓存、多连接感知、BroadcastEquipInfo |
| PetManager.cpp/h | **已完成** | 2019宠物系统扩展：ID缓存、排名缓存、多连接感知 |
| WorldBoss.cpp/h | **已完成** | 2019世界Boss逻辑：DB持久化、掉落记录、Boss升级、遗迹Boss |
| EquipBack.cpp/h | **已完成** | 2019装备找回：Init DB加载 + 多连接感知 + TranseCurrencyItem |
| FamilyManager.cpp/h | **已完成** | 2019家族系统：副本系统 + 领地/勋章 + OnInitFamilyInfo |

### 任务 C: 00_globals.cpp 全局函数
- 反编译源中包含全局工具函数，需审查项目引用

### 任务 D: 编译错误修复（最终阶段）
- 统一修复缺少头文件、API名称不匹配、类型转换等问题

---

## 三、执行优先级建议

1. **先做任务 B** — 逐文件对照反编译源完善业务逻辑
2. **任务 C** 随时穿插 — 遇到缺失全局函数时补充
3. **任务 A** 可渐进 — 按需重写反编译风格函数
4. **任务 D** 最后统一处理

---

## 四、关键参考路径

- 反编译源: `/home/gameserver/decompiled_backup/`
- 配置文件: `/home/gameserver/gameserver/ServerConfig/`
- CfgData Init函数: 247个已实现（全部声明已覆盖）
- CfgData.cpp: 24685 行 | CfgData.h: 7889 行

### 14. ItemEffect 6个TODO子类 — 已完成
- BackCityPaper: m_MapId/m_X/m_Y → m_itemid/m_BXinMo，effect实现心魔/普通回城逻辑
- RandomPosPaper: m_MapId → m_itemid，effect调用 OnRandPos(0)
- ShouChongItem: m_Index → Value，effect实现首充记录检查（getRecord(37505) + updateRecord + RecalcAttr）
- SummonBoss: m_BossId → m_pCfgMapMonster/m_pCfgMonster，effect实现BOSS召唤+世界广播（0x2CD6）
- VipDrop: m_DropId → m_Type，effect调用 CVip::AddVipLuckyDrop(m_Type, count)
- YanHua: 完整实现随机礼包抽奖（概率/static_probability双模式+职业/等级过滤）+烟花广播
- Player.h: +OnBackCity(Slot, bXinMo) 重载 + BroadcastYanHua(nType) 声明
- Player.cpp: +OnBackCity 2参数版实现 + BroadcastYanHua 实现（方向2格广播0x2829）
- Vip.h: +VipLuckyDrop结构 + AddVipLuckyDrop方法 + m_VipLuckyDropMap成员
- Vip.cpp: +AddVipLuckyDrop实现（Type1=普通次数+limit1906, Type2=特殊次数+limit1907）
- Map.h/cpp: +GetDirTileByDistance静态方法（方向N格计算）
- CfgData.h: CfgItemGiftRandom合并为单一完整定义（+static_probability/sum_probability），删除旧版重复定义
- ItemEffect.h: +CfgMapMonster/CfgMonster前置声明
- ItemEffect.cpp: +OpenBeta.h/Vip.h include

### 15. Player::PayedDispose 重构 — 已完成
- 从空壳扩展为完整充值分发逻辑（64行）
- 已集成系统：m_PlayerYunYingHD, GetOperateLimit, GetPlayerVip, OPEN_BETA
- 已实现功能：累计充值统计、操作限制、超级会员充值、每日充值图标、公测图标、VIP经验增加
- 待实现子系统（TODO标记）：月抽奖、跨服充值、双十一活动、国庆活动、联合服务器、开服活动、奖励大厅、活跃度
- Player.cpp: 从1478行更新，新增GetTodayPayGold()调用（原m_todayGoldCharge成员已存在）

### 任务 B: 已修改文件 2019 逻辑完善（续表）

| 文件 | 状态 | 说明 |
|------|------|------|
| Player.cpp | **已完成** | 从97行空壳扩展到~680行完整实现；所有存根方法替换为反编译源对照的干净实现；新增50+网络包处理器和业务方法 |
| Player.h | **已完成** | 新增m_connid/m_PlantState/m_bDie/m_isBackCity成员；新增80+方法声明（含LevelUped双参数重载）；initNetPacketHandlers2019声明 |
| Vip.cpp/h | **已完成** | 从48行空壳扩展到~1260行完整实现；43个方法全部对照反编译源实现；新增VIP卡系统(3种卡)、Club系统、LuckyDrop系统、VIP属性/特权 |

### 任务 C: 全局函数审查（00_globals.cpp）— **已完成**
- 已确认31个全局函数类型（CRT入口4个 + 比较谓词9个 + 槽位检查8个 + 工具函数6个 + 服务器初始化8个）
- 项目中已存在：sortInt, IsGoblinType, GetWeekDay, roundInt, IsInvalidSlot
- 已实现：GreaterItem, GreaterBagItem, comp, IsFaBaoType, IsShouHunType, IsWingEquipSlot, IsMoFuSlot, IsVipEquipSlot, isXingMaiSlot, GetLogItemFront, GetDayBattle, getUserIndex, GetServerVersion
- 创建了 00_globals.cpp 和 00_globals.h 文件实现所有缺失函数

### 任务 D: 调用端 connid 签名更新 — **部分完成**
- EquipManager.cpp: CreateMemEquip 修复 connid 参数传递（第94行）
- PetManager.h/cpp: AddPet/DelPet/UpdatePet 增加 connid 参数，添加兼容重载
- 更新所有调用点（CharPet.cpp 7处，CharInsidePet.cpp 1处）
- Vip.cpp: 6处 OnSendSysMail 调用更新为 connid 感知
- ShiZhuang.cpp: 1处 OnSendSysMail 调用更新
- EquipBack.cpp: 1处 OnSendSysMail 调用更新
- 遗留问题：FamilyWar.cpp 中3处 OnSendSysMail 调用传递邮件ID而非玩家ID，需要进一步调查
- GameService.cpp: onNewMinuteCome 调用保持 connid=0（系统级别定时器）
