# 2026-06-07 项目状态 & 任务清单

## 一、已完成工作

### 1. 缺失类补全（第一轮+第二轮）— 全部完成
- DBData.h/cpp, GameLogs.h/cpp, ItemEffect.h/cpp 扩展
- Curse.h/cpp, MutiConn.h 新增
- 所有反编译源中的类均已实现

### 2. 2019版本字段修正 — 全部完成
- ItemEffect 8个子类字段修正 + 新增 LingZhu, EquipJinHua

### 3. CfgData 重复代码清理
- 删除23个重复的 Init 函数定义（反编译伪代码 vs 2019干净版）
- 删除 2472 行冗余代码
- CfgData.cpp 从 25855 行降至 24685 行

### 4. CfgData Init 函数全部实现
- 42个之前缺失的 Init 函数全部实现为干净版
- 当前总计 247 个 Init 函数实现（含 int32_t 返回值的 InitXuWuTask/InitXinMoTask）
- cfgdata_new_decls.h 中所有声明均已实现
- 新增26个配置结构体到 CfgData.h
- 新增19个成员变量到 CfgData 类

### 5. 其他已完成
- CDBCFile.h/cpp 新增
- DataStructs.h 扩展（WorldBossInfo, KillerInfo, PlayerScoreRankGreater, MGLog）

### 6. DBService 2019多连接架构重构 — 已完成
- DBService 从继承 TcpService 改为管理多个 MutiConn<DBService> 连接
- MutiConn.h 重写为 TcpClient 子类，支持多连接转发
- DBService.h 重写：新增 ~30个函数声明，所有函数增加 int8_t connid 参数
- DBService.cpp 完全重写（~1700行）：onNetPacket 从 13 个 case 扩展到 28 个 case
- 注意：调用端（~20+文件）需逐步更新签名匹配

### 7. GameService 2019多连接架构重构 — 已完成
- GameService 从单连接改为管理多个 MutiConn<GameService>（m_mConn map）
- onNetPacket 签名改为 (GameConn* pConn, NetPacket* inPacket)，非virtual重载
- 新增 IndexMap, ChatValidate 结构体，MAX_GATE_CONNS=100
- 新增成员：m_id, m_userLock, m_ChatValidateMap, m_ChatValidateLock, m_dropControl, m_dropControlLock, m_serverBattle
- m_users 扩展为 [MAX_GATE_CONNS * MAX_CONNECTION] 支持多gate
- 新增函数（~40个）：
  - 网络：GetConn, GetAutoConnId, GetConnId, getUser, getUserPlayer, OnDBNetPacket
  - 发送：sendPacket(connid), sendPacketTo(connid), replySuccess, replyfailure
  - 广播：broadcastActivityState, broadcastToFamily, broadFamilyWarIcon, broadCastKiaFuHuoDongIcon, SendOpenBetaIcon, FamilyBroadcast
  - 业务：OnDaySwitch, onUpdatePay, RecalAllPlayerAttr, ResetFestivalData
  - Drop控制：InitDropTimes, GetDropTimes, AddDropTimes, UpdateDropTimes, ResetDropTimes, ClearDropTimes, OnDropTimesLoaded, CheckDropLimit, OnDropRecordLoaded, OnDropRecordSaved
  - Chat验证：SendChatValidate, GetChatValidate, CheckChatValidateChatSend, OnChatValidateResult
  - 城战：UpdateCityWarTitle, UpdateCityActState, SetFamilyMemberNeedSync, GetFamilyMemberInMap, broadcastCityWarWinnerLogin
  - 世界Boss/跨服：InitServerBattle, SetServerBattle, InitMoYuShiJieRecord, SendMoYuShiJieRecord, AddMoYuShiJieRecord
  - 禁言/踢人：OnSocialCityMasterBanChat, GetCityMasterBanChatTime, KickAll
  - 其他：onCheckTeShuTitle, UpdateTencentInfo, SetWorldLevel, onUpdateActivityState
- onNetPacket 消息处理扩展到 22 个 case（含 0x4E81-0x4E8E, 0x4EC1, 0x4EC4）
- 旧函数签名兼容：popNetpacket(connid), broadcast(IndexMap), onPlayerSaved(connid,...)

### 8. EquipManager 2019装备系统扩展 — 已完成
- 新增 ServerNewId 结构体（nNextId/nMaxId），ID缓存机制避免每次查DB
- 新增 m_mNewId map + m_IdLock 读写锁
- UpdateMemEquip/DeleteMemEquip 增加 connid + nReason 参数（保留旧签名兼容重载）
- SendPlayerEquipInfo 重构为3个重载：Player指针版 + 单装备版 + 向量版
- 旧 `const Player&` 版本保留为内联兼容包装
- 新增 BroadcastEquipInfo — 世界广播装备信息
- getEquipId 改为缓存机制 + getEquipIdFromDB 批量获取ID范围
- getEquipIdFromDB 区分跨服/非跨服模式（line==9时serverId=0）
- 所有发送函数使用 MemEquip::PackageClientData + 多连接感知发送（connid+gateIndex）

### 9. PetManager 2019宠物系统扩展 — 已完成
- 新增 PetNewId 结构体（同 EquipManager 的 ServerNewId），m_mNewId + m_IdLock 缓存宠物ID
- 新增 m_mPetRank（map<PetId_t, int16_t>）— 宠物排名缓存
- 新增 GetPetRankIndex(nPetId) — 查排名索引
- OnUpdatePetRank/OnInitPetRank 重写：简化为 PetId+RankIndex（不再读取10个属性排名）
- getPetId 改为缓存机制 + getPetIdFromDB 批量获取ID范围
- getPetIdFromDB 区分跨服/非跨服模式（line==9时serverId=0）
- DB_SERVICE.insertMemPet/updateMemPet/deleteMemPet 调用已适配 connid 参数

### 10. WorldBoss 2019世界Boss逻辑重构 — 已完成
- 新增 InitDBInfo — 从 mem_world_boss_info 加载Boss等级/经验/击杀者到内存
- 新增 InitDropRecord — 从 mem_drop_record 加载掉落记录（special 5条 + 普通 30条）
- 新增 m_dropRecords / m_dropRecordsSpecial / m_RecordLock
- 新增 DropRecord 结构体（strName, nCharId, nMapId, nMid, nRecord, nTime, nSpecial）
- Init 增加 InitDBInfo/InitDropRecord 调用，使用 CfgData::GetMonsterReviveTime
- OnAddBoss — 创建Boss怪物（含等级属性调整 adjustBossAttr）
- OnBossSummon — Boss复活，签名增加 Map*，增加 saveBossInfo
- OnBossKilled — Boss被杀，签名增加 Map*，含等级提升、RuinsBoss通知、saveBossInfo
- adjustBossAttr — 根据等级调整Boss属性（映射vAttr到CfgMonster字段）
- saveBossInfo / UpdateWorldBossInfo — DB持久化
- AddDropRecord / SendDropRecord — 掉落记录添加和发送（多连接）
- SendRuinsBossInfo / UpdateRuinsBossInfo — 遗迹Boss信息（多连接）
- PacketBossInfo 移除 m_IsShow 检查
- SendWorldBossIcon 改用 Player::SendIconState
- CfgData.h: 新增 GetMonsterAdjust/GetMonsterReviveTime 声明，m_mMonsterAdjust key改为pair<int,int>
- CfgDropRecord 新增 nSpecial/nItemClass 字段
- Monster.h/cpp: 新增 OnLevelUp 方法
- PDUDefine.h: 新增 SM_SEND_DROP_RECORD/SM_SEND_RUINS_BOSS_INFO/SM_SEND_RUINS_BOSS_CHANGE

---

## 二、待完成任务

### 任务 A: 反编译风格函数重写（93个）
CfgData.cpp 中仍有93个反编译风格的 Init 函数（含 IDA 伪代码标记如 v1; // rax）
这些函数功能正确但代码质量差，需要逐步重写为干净版本
优先级：低（功能已有，可渐进式优化）

### 任务 B: 已修改文件 2019 逻辑完善

| 文件 | 状态 | 说明 |
|------|------|------|
| DBService.cpp/h | **已完成** | 2019多连接架构重构完成 |
| GameService.cpp/h | **已完成** | 2019多连接架构重构完成：~40新函数，多gate支持 |
| EquipManager.cpp/h | **已完成** | 2019装备系统扩展：ID缓存、多连接感知、BroadcastEquipInfo |
| PetManager.cpp/h | **已完成** | 2019宠物系统扩展：ID缓存、排名缓存、多连接感知 |
| WorldBoss.cpp/h | **已完成** | 2019世界Boss逻辑：DB持久化、掉落记录、Boss升级、遗迹Boss |
| EquipBack.cpp/h | 待做 | 装备找回系统 |
| FamilyManager.cpp/h | 待做 | 家族系统 |

### 任务 C: 00_globals.cpp 全局函数
- 反编译源中包含全局工具函数，需审查项目引用

### 任务 D: 编译错误修复（最终阶段）
- 统一修复缺少头文件、API名称不匹配、类型转换等问题

---

## 三、执行优先级建议

1. **先做任务 B** — 逐文件对照反编译源完善业务逻辑
2. **任务 C** 随时穿插 — 遇到缺失全局函数时补充
3. **任务 A** 可渐进 — 按需重写反编译风格函数
4. **任务 D** 最后统一处理

---

## 四、关键参考路径

- 反编译源: `/home/gameserver/decompiled_backup/`
- 配置文件: `/home/gameserver/gameserver/ServerConfig/`
- CfgData Init函数: 247个已实现（全部声明已覆盖）
- CfgData.cpp: 24685 行 | CfgData.h: 7889 行
